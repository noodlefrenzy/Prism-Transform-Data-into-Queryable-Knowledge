# Image Assets

This folder contains images used in the documentation.

## Required Images

### 1. `chatscreen.png` (README hero image)
Screenshot of the Query interface showing:
- A question being asked about documents
- AI-generated answer with citations
- Source document references with page numbers

### 2. `architecture.png`
Architecture diagram showing the three layers:
- Document Processing (PDF/Excel/Email → Local libs → AI enhancement → Markdown)
- RAG Pipeline (Deduplicate → Chunk → Embed → Index)
- Query Layer (Knowledge Agent → Answer synthesis → Citations)

Include Azure service icons where appropriate.

Suggested tool: [draw.io](https://draw.io), PowerPoint, or Figma

## Optional Images

### 3. `workflow.png`
Screenshot of the Workflows view showing:
- Section list with question counts
- Progress indicators
- Run/export buttons

### 4. `results.png`
Screenshot of the Results view showing:
- Answered questions with citations
- Evaluation scores (relevance, coherence, fluency, groundedness)
- Export to CSV option

### 5. `pipeline.png`
Screenshot of the Pipeline view showing:
- Pipeline stages (Process, Deduplicate, Chunk, Embed, Index)
- Status indicators

## Video

Consider recording a short demo video (2-3 minutes) showing:
1. Uploading documents
2. Running the pipeline
3. Querying with citations
4. Running a workflow

Host on YouTube and link from README.

## Image Guidelines

- PNG format
- Width: 800-1200px
- Crop to show relevant content
- Blur/redact sensitive data
- Use clean browser window (no bookmarks bar)
